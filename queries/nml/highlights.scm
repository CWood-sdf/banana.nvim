; inherits: nml_tags

(doctype) @constant

"<!" @tag.delimiter

(entity) @character.special

(substitution) @character.special

(conditional) @keyword.conditional

(templating_cont_open) @punctuation.bracket
(templating_open) @punctuation.bracket
(templating_close) @punctuation.bracket
(templating_close_start) @punctuation.bracket

(comma) @punctuation.delimiter

(loop_in) @keyword

(named_variable) @variable
