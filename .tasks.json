{
	"tasks": [
		{
			"name": "test",
			"cmd": "./test",
			"tags": ["test"],
			"window": {
				"name": "banana test",
				"close": false
			}
		},
		{
			"name": "crash",
			"cmd": "nvim test.nml",
			"window": {
				"name": "ookc",
				"close": false
			}
		},
		{
			"name": "run",
			"cmd": "nvim -c \"lua vim.defer_fn(function() require('banana').test.grid() end, 500)\"",
			"window": {
				"name": "ook",
				"close": true
			}
		},
		{
			"name": "debug",
			"cmd": "nvim -c \"lua vim.defer_fn(function() require('osv').launch({ port = 8086 }) end, 200)\" test.nml",
			"window": {
				"name": "ookd",
				"close": true
			}
		},
		{
			"name": "zig debug",
			"cmd": "/usr/local/bin/nvim -c 'so zigtestpre.lua' bench/dst/0.lua",
			"window": {
				"name": "ookzd",
				"close": true
			}
		},
		{
			"name": "all debug",
			"cmd": "/usr/local/bin/nvim -c 'so zigtestpre.lua' test.nml -c \"lua vim.defer_fn(function() require('osv').launch({ port = 8086 }) end, 200)\"",
			"window": {
				"name": "ookad",
				"close": true
			}
		},
		{
			"name": "benchmark",
			"cmd": "perf record -F 999 -a --call-graph dwarf -g nvim -c 'so zigtestpre.lua' test.nml",
			"window": {
				"name": "bench",
				"close": false
			}
		}
	]
}
